spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 200ms
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2
          max-wait: 100ms
  cache:
    redis:
      time-to-live: 10m
      cache-null-values: false